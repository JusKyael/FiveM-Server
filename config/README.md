# Server Configuration Templates
This directory contains template configuration files and environment schemas. 

## Security Note
Actual production credentials (IPs, Passwords, API Keys) are stored in local `.cfg` and `.env` files which are protected by the `.gitignore` to prevent data leaks.

## Content
- Database schema templates.
- Resource manifest configuration examples.
- Environment-specific variables.
